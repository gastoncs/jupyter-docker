services:
  jupyter:
    build:
      context: .
    ports:
      - 8889:8888
    volumes:
      - type: bind
        source: ./app
        target: /home/jovyan/
    command: start-notebook.py --NotebookApp.token='my-token'

volumes:
  app:
    name: app